#include <stdio.h>

int main(int argc, char const *argv[])
{
    int a, b, c;
    printf("Enter a number a:");
    scanf("%d", &a);
    printf("Enter a number b:");
    scanf("%d", &b);
    printf("The sum is %d\n", a + b);

    return 0;
}
